# Agent Change Log

## Task objective

End flight / смерть от метеорита не должны зависать: не вызывать `meteorSystem.stop()` из `GameScene.onShutdown` (disableBody во время SHUTDOWN).

## Files created or modified

- `src/scenes/GameScene.ts`
- `logs/2026-08-20_22-30-41_git_log.md`

## Key changes

- `onShutdown`: audio off, ESC off, POST_RENDER off, `starfieldSystem.stop()`. `meteorSystem.stop()` убран.
- `onPostRenderLeave`: без изменений — `meteorSystem.stop()` после `world.resume()`.
- `MeteorSystem.stop` не менялся: уже как WeaponSystem (deactivate тел только если сцена active).

## Verification

- `npm run typecheck` — успешно.
- Запуск игры — не проверялся (точечный фикс shutdown).
- Ручная проверка — не выполнялась.

## Status

Ready for review.
