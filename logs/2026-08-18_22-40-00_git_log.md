# Agent Change Log

## Task objective

Исправить отсутствие движения и стрельбы: Phaser 3.90 не имеет `game.hasFocus`, из-за чего ввод всегда отбрасывался.

## Files created or modified

- `src/managers/InputManager.ts`
- `src/systems/WeaponSystem.ts`
- `src/scenes/GameScene.ts`

## Key changes

- Ввод больше не проверяет несуществующий `game.hasFocus`.
- Пауза ввода только если вкладка скрыта (`document.visibilityState`).
- Канвас получает `tabindex` и focus при старте GameScene.

## Verification

- `npm run typecheck` — pending.

## Status

Ready for review.
